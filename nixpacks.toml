[phases.setup]
nixPkgs = ["nodejs-20_x", "npm"]

[providers]
node = "20"

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

